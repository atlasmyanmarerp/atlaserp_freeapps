<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_move_line_detailed_operation_tree_inherit_slngapi" model="ir.ui.view">
        <field name="name">stock.move.line.operations.tree.inherit.slngapi</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_stock_move_line_detailed_operation_tree" />
        <field name="arch" type="xml">
            <field name="product_id" position="after">
                <field name="sl_product_tag" widget="many2many_tags" />
            </field>
        </field>
    </record>

</odoo>
